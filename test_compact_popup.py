# Test compact threat notification popup\n\nimport sys\nimport os\n\n# Ensure the project root is in sys.path\nproject_root = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))\nsys.path.append(project_root)\n\nfrom gui.popup_alerts import show_compact_threat_notification\n\n# Mock parent (None) – the function expects a parent QWidget, but we can pass None for testing\n# Provide a sample threat data dict\nthreat_data = {\n    'title': 'Phishing Attempt Detected',\n    'description': 'Suspicious phishing site detected and blocked.',\n    'level': 'HIGH',\n    'icon': 'exclamation-triangle'\n}\n\n# Call the function – it will display a Qt dialog if possible\nresult = show_compact_threat_notification(threat_data, None)\nprint('Compact notification result:', result)\n
